어셈블리어로 작성된 명령어는 주기억장치에 저장되어 있다가, CPU가 하나씩 레지스터로 가져와 실행한다.

mem = 메모리

data세그멘트 = 데이터가 들어가는 명령

실행파일을 만들면 = 보조기억장치에 저장셈블리어로 작성된 명령어는 주기억장치에 저장되어 있다가, CPU가 하나씩 레지스터로 가져와 실행한다.


<img width="473" height="670" alt="image" src="https://github.com/user-attachments/assets/3ca64e3c-7a26-46a9-873f-16291f2b4092" />

어셈블리어로 제작된 명령어는 시작과 목적지 둘의 비트수가 동일해야함


명령은 다양한 사이즈가 존재하는데 4바이트짜리 명령어를 2바이트짜리 ip로 해석할떈 2바이트를 증가시켜서 해석하고 메모리에서 명령을 가져와서 실행함 
다음 명령은 ip 레지스터가 가지키고 있어야 명령을 실행할 수 있음

일반적으로 하나의 명령이 수행 끝나면 destination operand에 따라서 플레그레지스터의 비트를 교체함 

--------------------------------------------------------------------

*Overlapping Values*

<img width="910" height="233" alt="image" src="https://github.com/user-attachments/assets/7c8cb3bd-992e-451a-aae1-101836408853" />

EAX, AX, AH, AL등의 인접 레지스터는 독립된게 아니라 한 구역(EAX) 안에 겹쳐서 연결되어있음 < - “Overlapping Values(겹치는 값)"

--------------------------------------------------------------------

"Zero/Sign Extension"

MOVZX/MOVSX 명령어로 작동
[(16비트)] 데이터를

[(16비트)(16비트)] 공간으로 로 이동시킬때

MOVZX는 앞 16비트 공간을 0000으로 비워둠, 

MOVSX는 앞 부호에 따라 0000 or 1111

--------------------------------------------------------------------

"XCHG"

XCHG dest, src  == dest(대상) 과 src(원본) 의 값을 서로 교환(exchange) 한다

--------------------------------------------------------------------

Data-Related Operators and Directives

Operators: 명령 
Directives: 번역

--------------------------------------------------------------------

예제 vs코드에서 디버깅 해보기

중간고사: 각 쳅터 exercises문제 그대로 나옴 + 인의예지신중도

